# 2026-06-13

## Android status bar dark strip fix

- Scope: code-only fix for a user report where the APK showed a gray/black strip behind the top system status bar on one Android device. No APK version bump, release manifest change, or package publishing was done.
- Change: added `android/app/src/main/res/values/colors.xml` with the app paper surface color `#FAF9F5`, updated Android themes to use that color for status/navigation bars and light system bar icons, and updated `MainActivity` to set the window/decor/WebView background plus disable Android Q+ contrast scrims.
- Kept existing Android overscroll behavior and page safe-area/layout CSS unchanged.
- Verification: `JAVA_HOME` in the shell pointed to a missing `D:\Program Files\Java\jdk-23`; reran with temporary `JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot`. `android\gradlew.bat :app:assembleDebug` passed. Java printed a deprecation note for legacy system UI flags used for old Android compatibility.
- Next handoff: if the affected user can retest, confirm the top status bar background is light on their device after the next APK build. If it still appears gray, check whether their ROM is forcing dark mode/contrast and consider a small WebView edge-to-edge follow-up instead of changing page scroll structure.

## 年度回声 v1 本地入口

- Scope: implemented the first Annual Echo experience without user-facing push/reminder delivery. User explicitly said to pause pushing to users, so backend scheduled push, Android local notification, notification preference fields, and Settings reminder toggle were intentionally not added.
- Frontend: added `/annual-echo?year=YYYY` with an old-diary page-turn story, local stats, quote, annual reply, personal instruction manual, fallback state, refresh, and poster save. Added search virtual result so `2026年度报告` / `年度报告` / `年报` / `年度回声` can open the page even when no diary result matches.
- Data: added local IndexedDB `annualEchoDigests` cache through `diaryService`; digest source freshness uses the annual entries' `id + updatedAt + diaryDate` hash and stays local-only.
- AI boundary: local stats are deterministic; AI is only used when authenticated for quote selection, one annual reply, and evidence-backed `只要……我就……` manual items. Validation enforces original quote matching, evidence, sentence pattern, dedupe, and fallback.
- Verification: `npm run test:annual-echo`, `npm run lint`, and `npm run build` passed. Browser smoke on `http://127.0.0.1:3000/` verified search virtual result, annual page with fallback data, seeded multi-entry stats, manual expand/scroll, and desktop viewport.
