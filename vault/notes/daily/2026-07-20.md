# 2026-07-20

## 月度回声行动轨迹长标题错位修复

- 行动轨迹使用固定时间轴锚点；当第四条行动标题换成两行时，原场景文字会侵入下一条日期区域。本次问题出现在“整理工作空间时”与下一条日期重叠。
- 现在第四条行动标题超过 18 个字符时，场景短语仍保留在标题下方；第五条日期、标题与叶子节点整体下移，左侧绿色时间线同步延长。短标题布局、其它节点和页面总结保持不变。
- 遮盖旧叶子节点时使用带柔边的同源纸纹补片，并复用原图叶子节点，避免安卓高 DPR 画面出现矩形接缝或残留节点边缘。
- 390×844 使用同类长文案复测无重叠和横向溢出。截图：`tmp/monthly-echo-runtime/actions-long-title/actions-extended-390x844.png`。
- 验证通过：`npm run test:monthly-echo`（37 项）、`npm run lint`、`npm run build`、`git diff --check`。未发布 APK、未部署线上。
- Android `Pixel_8_Codex` 模拟器通过 `adb reverse tcp:4173 tcp:4173` 打开生产构建；Android Chrome 实际视口为 `412×784 / DPR 2.625`。场景短语与下一日期间距约 `8.87px`，第五条标题与总结卡间距约 `50.78px`，无重叠和横向滚动。设备截图：`tmp/monthly-echo-runtime/actions-long-title/actions-extended-android-verified-device.png`。
- 用户复核发现遮罩交界处仍有细小断口；补绘线现改为从心形节点下沿一直伸入叶子节点内部的单段实线，不再只覆盖旧线末端。Pixel 8 最终截图：`tmp/monthly-echo-runtime/actions-long-title/actions-line-continuous-android.png`。

## 月度真实情绪与行动证据回填

- 真机发现第二页无情绪、第五页只有一条行动。线上仍运行旧 Node `pid 11388`，旧报告没有按新版本重建。
- 本地真实七月数据证明证据已存在；丢失点是 DailyTrace 把部分本人行动只归到 importantEvents，以及“有些乱”等明确情绪短语未被标准化。
- 已增加确定性补回：只从校验过的原句中提取明确情绪；把 importantEvents 中本人完成的可观察行为补为 action；排除朋友、同事等第三方行为。版本为 `daily_trace_v2_4 / monthly_arc_v2_10 / monthly_echo_render_v2_11`。
- 验证：月度专项测试 41 项、lint、server build 通过；服务端最小文件集已上传，待宝塔重启后真实账号验收。

## 个人页心情趋势图表优化

- 原图表把正文粗分成三档、为空白日继承前值并加入日期随机偏移，导致低落无法可信地下穿平静线。现改为最近七天 `-2～2` 五级情绪轴，显式 `DiaryEntry.mood` 优先，正文情绪词只作确定性兜底；无日志或无情绪证据时保留 `null` 并让曲线断开。
- 曲线使用独立的单调三次 Hermite/Fritsch-Carlson 路径计算，控制点限制在相邻端点范围内；个人页新增响应式交互组件，支持默认最近有效日、点击、键盘和横向拖动选择、边缘详情避让、36px 命中区、深色模式与全空状态。
- 新增专项测试 `npx tsx tests/profile-mood-trend.test.tsx`，覆盖映射、模板标题清理、逐篇日均、空值分段、单点/重复值/极值、控制点限幅和交互可访问性，并已接入 `npm test`。
- 验证通过：`npm test`、`npm run lint`、`npm run build`、`git diff --check`。Puppeteer 在 390px、320px 和深色模式下验证无横向溢出、详情不遮挡节点、拖动与空状态正常；截图为 `tmp/mood-trend-390-light.png`、`tmp/mood-trend-320-light.png`、`tmp/mood-trend-320-dark.png`、`tmp/mood-trend-empty.png`。未修改后端、数据库或同步契约。
