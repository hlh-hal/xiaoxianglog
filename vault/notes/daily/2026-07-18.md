# 2026-07-18

## Android v1.0.23 月度回声 UI 发布

- 用户要求只发布月度回声 UI，当前主工作区同时含尚未上线的微信登录改动；本次使用隔离 Git worktree 构建，只带入月度回声页面、封面资源和行动轨迹空状态相关文件，未把微信登录代码打进 APK 或上传线上。
- 发布内容：第七页回声信连续正文流、封面与翻页提示细节、行动轨迹空状态；更新公告同步描述了正文重叠、署名小象裁切、地图/箭头错位等修复。
- 发布版本：`1.0.23 / versionCode 25`，主下载地址仍为 `https://xiaoxianglog.cn/download/xiaoxiang-log-latest.apk`。

## 验证与部署

- `npm run test:monthly-echo`：24 项通过；`npm run build`、`npm run android:sync`、`gradlew assembleRelease`：通过。隔离 worktree 的 `npm run lint` 因 Windows `node_modules` junction 解析缺少若干第三方类型定义而未通过，不属于本次代码错误；正式构建已成功。
- 正式 APK 已通过 v2/v3 签名校验，证书 MD5 为 `9a0e0281cd8b3070c425c22290fd3eb4`，与备案值一致；`aapt` 验证包名 `com.xiaoxiang.diary`、版本 `1.0.23 / 25`。
- 公网反向校验通过：远端 APK SHA256 为 `728A4123A0E64DD990EE6C722F67DB20444956D63D51BE46F6D04AB196ADEF43`，与本地一致；`app-update.json` 返回 `1.0.23 / 25`；APK 支持 HTTP Range。
- 前端 FTP 上传中 3 个未改动旧图标因超时未重复覆盖，但主 JS/CSS、`app-update.json` 和本次月度回声全部素材均上传完成；不影响本次功能。

## 月度回声首页文字边缘修复

- 真机截图中“月之回响”底沿和“一份温柔的”顶沿发虚，根因是动态月份遮罩的纸色阴影范围过大，并非底图分辨率不足。
- 将 `.cover-month-patch:before` 从 `inset:-7px`、`12px/9px` 大羽化收紧为 `inset:0`、`5px/3px` 小羽化；仍完整遮住底图原始月份，但不再覆盖上下相邻文字。
- 验证：`npm run lint`、`npm run test:monthly-echo`（24 项）和 `npm run build` 均通过；构建仅保留既有的大 chunk 与动态/静态导入提示。

## Android 模拟器复核

- 已启动隔离的 `Pixel_8_Codex`、重新构建前端、启动 `4173` 本地预览并建立 `adb reverse`；电脑端预览返回 200，模拟器处于 `device` 状态。
- Android 36 模拟器使用默认 `gfxstream` 时反复出现 `Process system/System UI isn't responding`；改为 `-gpu swiftshader_indirect` 后 Launcher 稳定。Chrome 首次启动仍触发一次 SystemUI ANR，按 UIAutomator 返回的真实 1080x2400 控件边界关闭弹窗后恢复正常。
- 月度回声首页已在 Pixel_8 模拟器 Chrome 中实际打开并截图：`tmp/monthly-echo-emulator/monthly-cover-verified.png`。确认“月之回响”上下边缘和“一份温柔的”顶沿清晰，月份遮罩没有侵入相邻文字，底部动态箭头位置保持不变。

## 月度回声第二页重复事件归纳

- 将第二页原来的逐条“日期 + 事件”改为三段式表达：`它出现在`、合并后的日期列表、`每一次都和“共同背景”有关。`。
- 共同背景直接从现有 `report.pages.recurring.lead` 提取；若没有主题背景，则回退到 `overview.initialQuestion`。未修改报告数据结构、后端生成接口或其他页面。
- 新增 `buildOverviewOccurrenceSummary` 行为测试；`npm run test:monthly-echo`、`npm run lint`、`npm run build` 均通过。
- 已在 Pixel_8 模拟器复核第二页，日期与共同背景自然换行且不遮挡后续内容。截图：`tmp/monthly-echo-emulator/monthly-overview-summary.png`。

## 月度回声行动页底部箭头平整修复

- 行动页底部动态箭头周围出现纸张“缺口”，根因是底图自带静态箭头，页面通过 `.exact-next-repair` 截取底图并叠加椭圆蒙版遮挡，补丁边缘在 Android Chrome 中可见。
- 新增 `public/monthly-echo/monthly-echo-actions-textless-v3.png`，仅移除底图最下方静态箭头并恢复连续纸张纹理；行动页改用 v3 素材，同时隐藏该页的 `.exact-next-repair`，动态箭头和其他页面保持不变。
- `npm run build` 通过，仅保留既有的大 chunk 与动态/静态导入提示。Pixel 8 模拟器在 `4173` 预览、`adb reverse` 环境下复核通过，截图：`tmp/monthly-echo-emulator/monthly-actions-flat.png`。

## 月度回声第六页证据日期与自适应排版

- 将反复主题引导句固定拆成两段结构：“当你……时，”与“你会很快开始问：”，不再依赖 AI 自己换行。
- 月度生成提示升级为 `monthly_arc_v2_5 / monthly_echo_render_v2_5`；`evolvedQuestion` 改为带 `evidenceIds` 的对象，转折日期只从新问题自己的有效证据解析。缺少独立证据日期时页面为 `partial`，不会套用最后一条旧事件日期。
- 第六页事件改为日期/事件双列时间轴；底部总结卡片取消行数截断，改为固定宽度、自动高度、normal/compact/dense 密度和极端长文内部滚动。两条与三条事件使用独立空间参数，容器边界不重叠。
- 验证通过：`node --import tsx tests/monthly-echo.test.ts`、`npm run lint`、服务端 `tsc --noEmit`、`npm run build`。Pixel 8 模拟器覆盖三条事件标准状态与两条事件长结论压力状态；最终压力截图为 `tmp/monthly-echo-emulator/monthly-recurring-v25-verified.png`。
- 后续微调两条事件状态：缩短时间轴占位并将三行转折说明上移，Android 模拟器测得转折文案容器与绿色问题卡之间保留约 `19.6px` CSS 间距；截图为 `tmp/monthly-echo-emulator/monthly-recurring-gap-fixed.png`。
