# 2026-05-25

## 摘要

- 发现共享记忆没有持续写入 `vault/` 的原因：规则仍允许只更新 `WORKLOG.md`，而 `vault/` 写入只是建议，不够强制。
- 修复方式：把 `vault/` 明确为跨会话交接源，新增 `vault/agent/memory-workflow.md`，并在 `AGENTS.md`、`vault/README.md`、`vault/agent/README.md` 中加入强制写入检查。
- AI 聊天「嘴硬知己」升级为「毒舌知己」：强化傲娇毒舌、情绪解码、低落降刺、拒绝模板化和系统提示词泄露边界。
- AI 聊天「温柔陪伴」升级为心理陪伴风格：先情绪镜像，再用简短心理学视角温和引导，保留危机边界和不替代专业咨询说明。
- AI 聊天「博学伙伴」升级为分析型陪伴风格：先抓问题核心，再用结构化分析、比喻和轻微幽默降低理解阻力，保留事实与专业边界。
- 已将前端 AI 风格改动上传到云端服务器，线上首页引用 `/assets/index-C_Xpqqdj.js`。

## 已补录的近期工作

- 本地日志文件夹同步：网页写入后读回校验，失败删除空壳；同步前清理 0B Markdown；移入/恢复回收站时按当前日记重新渲染 Markdown；Android 文件夹选择器默认引导 Documents。
- PWA 安装：增强安装条件检测、浏览器识别、手动安装步骤、安装模式和 service worker/manifest readiness。
- 通知：新增测试通知入口，兼容 callback/promise 两种 `Notification.requestPermission`，Android 通知设置跳转改用可点击 intent。
- 排行榜：点赞接口支持 `like`、`unlike`、`toggle`，前端显式发送目标动作，减少乐观更新和服务端状态不一致。
- 部署配置：nginx 片段扩展为完整站点配置，包含 SPA fallback、API/uploads 反代、manifest MIME、SSL 和静态资源类型。

## 验证记录

- 已在 `WORKLOG.md` 记录过：`npx tsx tests/local-vault-sync.test.ts`、`npm run lint`、`npm run build`、`npx cap sync android`、`android/gradlew.bat assembleDebug`。
- 本次记忆修复只改 Markdown 文档，未重新运行代码测试。
- 毒舌知己改动验证：`npm run lint`、`npm run build`、PowerShell 本地 10 轮场景审核均通过。构建仅出现既有 chunk size / dynamic import 警告。
- 温柔陪伴改动验证：`npm run lint`、`npm run build`、PowerShell prompt 检查均通过。构建仍仅出现既有 chunk size / dynamic import 警告。
- 博学伙伴改动验证：`npm run lint`、`npm run build`、PowerShell prompt 检查均通过。构建仍仅出现既有 chunk size / dynamic import 警告。
- 云端上传验证：`deploy.bat front` 成功上传 18 个前端文件；`curl -k -L https://47.122.112.242/` 确认引用新 JS；线上 JS 包含「认知共鸣机」「精准分析，知识储备惊人」「温柔而稳定的容器」「毒舌知己」；`/api/health` 返回 `build=cpamc-only-20260520`。

## 后续风险

- 这些改动目前仍在工作区未提交，包含 `AGENTS.md`、`vault/`、旧 `agent.md` 删除，以及多处源码变更。
- Windows 下 Git 显示旧入口为 `agent.md` 删除，新入口为 `AGENTS.md` 新增；提交时需要确认大小写重命名符合预期。
