# 2026-07-01

## 月之回响封面设计还原
- 用户提供了一张“月之回响 / June”封面截图，希望使用 `ky-design-to-html` 还原成项目内可运行的 HTML/CSS 页面。
- 新增 [src/pages/MonthlyEchoDesignDemo.tsx](/D:/小象日志/src/pages/MonthlyEchoDesignDemo.tsx) 并在 [src/App.tsx](/D:/小象日志/src/App.tsx) 增加 `/monthly-echo-design-demo` 路由，页面按 947×1661 比例做固定舞台缩放。
- 用户指出书本和右上圆环偏差后，改用完整参考图 [public/monthly-echo/monthly-echo-cover-reference.png](/D:/小象日志/public/monthly-echo/monthly-echo-cover-reference.png) 保留复杂主视觉；随后用户指出文字下方纸面遮罩形成突出的块，因此删除全部遮罩、阴影、额外噪点与重复可见文字，最终让所有视觉元素保持在同一张参考资产中。
- 页面仍保留屏幕阅读器专用的标题、月份、说明和祝福文案，避免纯图片页面丢失基本语义。
- 验证通过：根目录 `npm run lint`；本地开发服务器上使用 Puppeteer + 本机 Chrome，以 947×1661 视口打开 `http://127.0.0.1:3000/monthly-echo-design-demo`，页面画布实测为 947×1661，截图中无块状遮罩边界。
- 后续若要求文字可编辑或内容动态化，需要先准备无字底图及独立书本、花束、圆环素材，不再采用在有字截图上叠加遮罩的方案。
- 用户确认把最终封面用于月度回响正式首页，并要求除点击箭头外也支持上滑。`src/pages/MonthlyEcho.tsx` 的入口背景已切换到 `monthly-echo-cover-reference.png`，以 `contain` 等比显示；沿用原有纵向滚动容器，透明箭头区域允许纵向手势。
- 390×844 浏览器验证中，从箭头区域模拟上滑和点击箭头均进入第二屏，滚动位置均为 `844px`；生产构建与 TypeScript 检查通过。旧入口素材保留，回滚只需恢复 `entranceCoverBackground` 与背景尺寸配置。
- Android 模拟器验证出现“2026年7月月之回响 / 请求失败”。通过 WebView 调试协议确认设备在线、调试安装没有 access/refresh token，GET `https://www.xiaoxianglog.cn/api/monthly-echo?monthKey=2026-07` 返回 404 HTML：`Cannot GET /api/monthly-echo`；线上 `/api/health` 仍为旧构建 `cpamc-only-20260520`。本地 `server/src/index.ts` 已挂载该路由，因此根因是月报后端和 schema 尚未在线上生效，不是封面请求失败。
- 前端防护已补：当月报加载中、空数据或请求失败时，`MonthlyEcho.tsx` 仍把入口封面作为第一屏，状态页放在第二屏；成功月报内容结构不变。Puppeteer 模拟 404 验证第一屏 `hasCover=true`、状态不可见，上滑到第二屏后 `scrollTop=844` 并显示失败状态。
- 已重新执行 `npm run lint`、`npm run android:sync`、Gradle `:app:installDebug`，在 Pixel_8 模拟器重新安装并直接打开 `/monthly-echo?monthKey=2026-07`；实机截图确认新封面正常显示。线上真实内容仍需完成数据库 `db:push`、后端部署与服务重启后再验证。
