# 2026-07-14 月度回声 Android 验收

- 修复 `src/pages/MonthlyEcho.tsx`：当 V2 月报加载完成时，在下一帧将内部整页滚动容器复位到入口页，避免 Chrome 从 bfcache/后台恢复时停在不可见的吸附边界。
- 未调整七页的书本、干花、照片、纸张或其他视觉素材；只修正页面打开时的滚动状态。
- 本地生成的 2026-07 测试月报已为 `ready`，包含 11 篇明确标记为“月度回声测试”的测试日记；报告动态显示 July、11 篇，并在第 2 页验证了日志驱动的主题、日期和场景。
- 已通过：`npm run test:monthly-echo`、`npm run lint`、根目录 `npm run build`、`server npm run build`。
- Pixel_8 模拟器已运行，Chrome 停在 `http://127.0.0.1:3000/monthly-echo?monthKey=2026-07` 的入口页；本机 3000/3001 和 ADB reverse 均保持运行。AVD 重启后必须重新执行 `adb reverse tcp:3000 tcp:3000` 和 `adb reverse tcp:3001 tcp:3001`。
- 第二页采用“单页自适应密度”方案：不新增面向版面的 AI 字数限制，React 根据问题、场景和总结的总字符量切换 normal/compact/dense 三档字号与行高；问题、转折问题和总结不再使用 `line-clamp`，避免显示省略号。
- 第二页外框背景改为与 `monthly-echo-story-textless-v2.png` 上下边缘匹配的纸张渐变，保留 `390×844` 外框与 `390×693` 等比底图，不拉伸花、圆环和纸张纹理。
- 已重新生成当前测试账号的 `2026-07` 月报：regenerate 任务 success、报告 ready。真实第二页进入 compact 档，四个文字区块均满足 `scrollHeight === clientHeight`，浏览器截图为 `tmp/monthly-echo-emulator/overview-real-regenerated-route-b.png`。
- 第二页进一步利用上下留白：视觉层从 `390×693` 按原比例放大为 `429×762`，在 `390×844` 页面内居中，顶部和底部各约 `41px`；文字字号保持不变，百分比坐标随舞台自然拉开。Pixel_8 的 `411×808` 视口验证中，所有文字块仍满足 `scrollHeight === clientHeight`，最终截图为 `tmp/monthly-echo-emulator/overview-real-regenerated-spacious-mobile.png`，模拟器实机截图为 `tmp/monthly-echo-emulator/overview-real-regenerated-spacious-final-adb.png`。
- 第二页绿色转折问题的文字层单独向左 `6px`、向上 `7px`，用于对齐不规则绿色纸块的视觉中心；底图与其他文字块不变。
- 入口页应用与第二页一致的纵向画布修正：舞台由 `390×693` 等比放大到 `429×762` 并居中，上下留白约 `41px`；入口页外框单独使用从 `#f2e9dc` 到 `#eee4d8` 的渐变，匹配封面底图实测的上下边缘色，减少顶部和底部接缝。
- 入口页最终改为无接缝 cover：底图保持既有显示比例，以 `475×844` 从页面顶部铺到底部，左右仅裁切边缘纸纹，彻底遮住安卓视口原先暴露的左侧与顶部外框。动态英文月份、日记数和分隔线同步放大约 8%，维持与底图标题的字号比例；第二至第七页不受影响。
- Pixel 8 实测 cover 舞台相对视口向右偏约 `9px`，入口底图追加水平校正 `translateX(calc(-50% - 9px))`，使左右裁切更对称并保留更多右侧竖排文字。
- 为兼容 `390px` 设计画布与 Pixel 8 的 `411px` 可变容器，入口 cover 最终改用固定设计中心 `left: 195px` 配合 `translateX(-50%)`；在 390px 下不额外裁左侧标题，在 411px 下会自动抵消可变容器增加的半宽偏移。
- 第三页“本月地图”采用路线 B：舞台放大为 `429×762`，裁去底图约 `14px` 的纯白外沿，外框改为匹配地图纸张的 `#f5ebe0 → #f7efe6`；主线由两行提升为最多四行。主线、节点和总结使用 `min-height/max-height`，两条真实支线时应用 `map-count-2` 拉开纵向节奏，不新增或编造第三条支线。
- 地图页最终同样改为全高 cover：`475×844`、固定设计中心 `195px`，底图白沿裁切增至 `16px`。页面从顶部铺到底部，消除上下纸色色带；动态文字继续使用原字号和自适应高度，不随底图一起放大。
- 为兼容 `390px` 窄画布，地图主线和第一节点从 `left: 9%` 调整为 `12%`，主线宽度同步从 `72%` 收为 `69%`，避免全高 cover 后文字贴左边缘。
- 地图主线文字改为“外层 Flex 居中 + 内层四行 clamp”，因此短文在绿色纸块内水平、垂直居中，长文仍可自适应增高。无字地图素材误删的上节点至中节点曲线缺口，以 `#5d6e3d`、`1.35px` 的局部 SVG 贝塞尔线补齐，不覆盖或重画其他地图线。
- 补线截图校准后将路径端点扩展为 `M172 279 → 227 315`，与原曲线两端轻微重叠，避免抗锯齿边缘再次形成细小断口。
- 补线最终线宽调整为 `1px`，匹配底图原曲线的视觉粗细。
- 月度回声地图页主线文字垂直居中：将 `.map-main-patch` 从内容撑高的 `min-height/max-height` 改为覆盖绿色纸片的 `height: 13.5%`，继续使用 Flex 双向居中和四行限制，避免真实主线文案贴近纸片上沿。
- 地图页主线文字安全边界微调：仅将 `.map-main-patch` 的 `left` 从 `12%` 调为 `8%`，整体左移但保持宽高、字号和居中方式不变，减少左侧空隙并避免右侧文字贴边。
- 月度回声关键时刻页采用路线 B：将 `.exact-moments` 画布按原始 `941×1672` 比例改为 `475×844` 满高覆盖，消除上下色带；日期、事件文案和总结区改为 `height:auto + min-height/max-height`，事件文案使用 Flex 垂直居中、标题两行和正文四行限制，并将右侧文字区宽度收至 `44%` 避开照片资产。
- 关键时刻页第三张卡片长标题修正：第三个文案容器单独从 `top:58%` 下移到 `60%`，防止两行标题在 Flex 自适应高度下越过第三张纸片上缘。
- 关键时刻页左侧接缝与第三张卡片留白修正：根据原始素材可见边缘重新取样，以 `#f3eadf` 作为 `.exact-moments` 专属底色，并用同色 `box-shadow` 横向外扩覆盖 390px 画布在宽安卓视口留下的侧边缝；第三张日期与文案分别下移到 `59.5%`、`61.5%`，增加和纸片上缘的距离。
- 月度回声行动轨迹页采用路线 B：将 `.exact-actions` 画布改为 `475×844` 满高覆盖，使原素材连接线随页面纵向拉长并利用上下空间；页面底色取样为 `#f2e8dc` 并横向外扩消除侧边色差。行动项改为 `height:auto + min-height/max-height`，标题允许两行、正文最多三行；总结纸片下移至 `82%`，避免第五条行动进入总结区域。
- 行动轨迹第五条边界修正：将 `.action-copy-patch-5` 从 `top:69%` 上移至 `64%`，使日期、行动和场景回到叶子节点旁，并与 `82%` 开始的总结纸片保留明确间距。
- 行动轨迹下半区微调：第四条“短时间运动”从 `56%` 上移至 `54%`，第五条“调整重做”从 `64%` 调至 `63%`；总结文字改为内层 `span` 承担五行截断，外层卡片保留 Grid 双向居中并增加 `text-align:center`，避免内联 clamp 覆盖居中布局。
- 行动轨迹日期与总结纸片对齐：日期元素使用 `top:-18px` 和等量负下边距独立上移，使日期顶边与圆形图标顶边平行且不带动行动标题；总结文字层改为覆盖白纸实际区域的 `top:76%; height:17%`，在整张卡片内水平、垂直居中。
- 行动轨迹日期裁切修复：日期上移后会被 `.action-copy-patch` 的 `overflow:hidden` 裁掉上半部，因此外层改为 `overflow:visible`；标题两行和正文三行仍由各自的 line-clamp 控制，不放开文字长度边界。
- 行动轨迹总结卡片留白微调：保持整张白纸的 Grid 居中基准和水平居中，将总结内层文字额外 `translateY(-16px)`，使文案落在卡片中上区域，减少上半部空旷感。
- 月度回声反复主题页视觉修正：将 `.exact-recurring` 加入 `475×844` 满高画布组，利用横向自然裁切移除原素材四角黑色标记并消除上下色带；页面边缘取样 `#f5eadd` 并横向外扩。金色问题与绿色变化问题统一调整为 `15px/1.55`，与上下正文排版保持一致。
- 反复主题页证据条数自适应：为页面增加 `recurring-count-N` 类；当前只有两条日期证据时，仅将转折文字上移到 `51%` 填补中段空洞。绿色问题和总结纸片属于原图固定资产，继续保持 `70% / 83%`，避免只移动 HTML 文字造成资产错位；三条证据布局保持原位。
- 反复主题页两证据素材联动：为 `recurring-count-2` 仅裁出原图 `68%-82%` 的绿色问题纸片并上移 `9%`，对应文字移至 `61%`；原绿色位置使用同一原图 `54%-68%` 的纯纸张纹理覆盖。白色总结纸片和底部箭头保持原位，避免整体移动下半段产生横向色带；三证据布局不启用该切片。
- 反复主题页总结纸片继续收紧：单独裁出原图 `82%-94%` 的白色总结纸片与胶带并上移 `7%`，对应总结文字移至 `76%`；原位置同样用原图纯纸张纹理覆盖，底部箭头不移动。这样绿色纸片与总结纸片之间不再保留大段空白。
- 月度回声信页按参考图重排：`.exact-letter` 改为 `475×844` 满高画布，边缘纸色取样 `#f7efe5` 并横向外扩；照片、干花、胶带和信纸仍由原始无字 PNG 承担。正文移除容器级 line-clamp，恢复自然段流式排版，字号/行高改为 `12px/1.75`；总结句改用内层 span 保留三行截断且不覆盖 Grid 居中；称呼区加宽，签名区改为 `27%` 宽并禁止换行，确保真实昵称和“爱你的小象”完整显示。
- 月度回声七页由浏览器原生 `scroll-snap` 改为可交互 H5 纵向轨道：Pointer Events 拖拽期间页面按手指位移实时跟随，松手根据 `10%` 视口距离或滑动速度决定翻页/回弹，首尾页应用非线性阻尼且不暴露空白；落位使用 `360ms cubic-bezier(0.22, 1, 0.36, 1)`，新手势可中断动画，减少连续滑动偶尔失灵的僵硬感。
- H5 翻页继续兼容底部箭头、鼠标滚轮、方向键/PageUp/PageDown/Home/End，并为隐藏页应用 `inert`、增加页码屏幕阅读器播报和 `prefers-reduced-motion` 降级。纯手势判定放在 `src/utils/monthlyEchoPager.ts`，月报测试覆盖距离阈值、速度轻扫、首尾边界和阻尼。
- Pixel 8 模拟器验证：短拖回弹、快速上滑、拖动中上下页连续同屏、反向下滑、底部箭头前进和末页越界回弹均通过；截图位于 `tmp/monthly-echo-emulator/h5-pager/`。
- 验收结束后为清理 Chrome 冷启动 ANR 重启 AVD，`Pixel_8` 的旧运行快照与后续 `-no-snapshot-load` 冷启动均未在 3 分钟内完成系统启动；未使用会清空账号/应用数据的 `-wipe-data`。该问题发生在上述热态 H5 手势验收之后，后续若继续用模拟器需先修复 AVD 启动环境或新建不覆盖原数据的测试 AVD。
