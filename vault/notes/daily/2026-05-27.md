# 2026-05-27

## 日志编辑叶笺背景上线

- 来源：用户要求删除截图图二里的两个日志编辑背景，并添加图一作为新的日志编辑背景，其他不变，执行完上传云端服务器。
- 状态：已从 `src/types/theme.ts` 的编辑器主题列表移除 `sys-green-flower` 和 `sys-cute-flower`，新增 `sys-botanical-paper`（`/themes/botanical-paper.jpg`）；保留此前新增的 `midnight-indigo` 纯色主题。新图片来自用户提供的本地文件，已复制到 `public/themes/botanical-paper.jpg`。`src/pages/Editor.tsx` 的导出卡日期分割线改为按主题文字颜色选择深/浅线，避免浅色图片背景上分割线过淡。
- 验证：`npm run lint` 和 `npm run build` 通过；Puppeteer 手机视口打开 `/editor` 并设置 `lastUsedDiaryThemeId=sys-botanical-paper`，确认新背景加载、旧 `green_flower`/`cute_flowerjpg` 未作为编辑器背景出现、无 Vite overlay 和控制台错误。
- 上线：已执行 `deploy-upload.ps1 -Target front` 上传前端 `dist` 到 `47.122.112.242:/dist`，线上入口引用 `assets/index-BVsgPNlQ.js`；`/themes/botanical-paper.jpg` 返回 200；线上 JS 包包含 `sys-botanical-paper` 和 `/themes/botanical-paper.jpg`，不包含 `sys-green-flower` / `sys-cute-flower`。
- 纠正：用户指出第一次上传的 `botanical-paper.jpg` 不是目标图。已用用户指定的 `6174a338327b908aa9c1c93fc0dd970a.jpg` 覆盖 `public/themes/botanical-paper.jpg`，重新 `npm run lint`、`npm run build` 并再次执行 `deploy-upload.ps1 -Target front`。线上 `/themes/botanical-paper.jpg` 当前大小为 `1023799` 字节，SHA256 为 `B4809ADA4B3F251B954DAD17B32B8CF99543255E9FF95613E93C1E78D6C53BB2`，与本地源图一致。
- 二次纠正：用户已自行在 `public/themes` 更新目标背景，实际文件为 `botanical-paper.png`。已将 `src/types/theme.ts` 的 `sys-botanical-paper.backgroundImage` 改为 `/themes/botanical-paper.png`，重新 `npm run lint`、`npm run build` 并上传前端。云端已删除旧 `/dist/themes/botanical-paper.jpg`、`green_flower.jpg`、`cute_flowerjpg.jpg`；线上 `/themes/botanical-paper.png` 返回 200，大小 `2478619` 字节，SHA256 `94F9F35578BAEF316B1B0CF6E829189EE1ED769CC389FFBF6CB5E433D407F26C`，线上 JS 包 `assets/index-p9Y1nbFY.js` 只引用 PNG，不包含旧两个主题 ID。

## 日志编辑主题纯色背景

- 来源：用户提供两张主题面板截图，要求日志编辑界面新增对应的纯色背景，并在本地启动前后端查看效果；随后明确要求删除浅粉 `#F1E7E6`。
- 状态：已在 `src/types/theme.ts` 的编辑器主题数组保留 `midnight-indigo`（`#26306E`），删除 `soft-blush`；同步在 `src/config/themes.ts` 的旧背景选择/分享卡配置里仅保留 `solid-midnight-indigo`。
- 验证：`npm run lint` 通过；本地前端 Vite 已在 `http://localhost:3000` 运行，后端 dev server 已在 `http://localhost:3001` 运行；Puppeteer 验证首页和 `/editor` 无 Vite overlay、无控制台错误，并确认新建日志使用 `midnight-indigo` 时页面实际应用 `rgb(38, 48, 110)`。

## 首页批量删除

- 来源：用户反馈首页进入多选后点击“删除”没有反应，预期应移入回收站。
- 状态：已修复。`src/pages/Home.tsx` 在点击批量删除后先退出多选、从首页列表乐观移除选中日志，并显示“已移入回收站 N 篇”；`src/services/diaryService.ts` 新增 `moveEntriesToTrash()`，一次性把选中日志写成 `status: 'trashed'`、`trashReason: 'deleted'`、`trashedAt` 和 `updatedAt`，清理首页缓存并触发同步。
- 验证：Puppeteer 手机视口走首页长按、多选、选择第二篇、删除流程；两篇测试日志 IndexedDB 状态为 `trashed`，第三篇保持 `active`；成功截图 `home-bulk-delete-success.png`。
- 注意：`npm run lint` 当前仍被既有 `src/pages/Settings.tsx:92` 的 `PwaPushSubscriptionResult.reason` 类型错误阻断，和本次首页批量删除链路无关。

## PWA 通知开关无反应

- 来源：用户反馈更新后“打开通知”按钮点不动，目标是 Chrome 添加到桌面的 PWA 在前台、后台、锁屏都能正常收到写日志提醒、好友申请、点赞和评论系统通知。
- 状态：已修复前端订阅入口。`src/utils/notify.ts` 的 PWA Push 订阅增加 Service Worker 自动注册/更新、8 秒超时、清晰失败原因、VAPID 公钥不一致时重建旧订阅，以及订阅保存超时处理；`src/pages/Settings.tsx` 通知开关增加忙碌态、失败 toast 和服务端偏好同步；`public/sw.js` 缓存版本升到 `xiaoxiang-pwa-v4`，帮助桌面 PWA 更新到新的 push/click 逻辑。
- 验证：`npm run lint`、`npm run build`、`cd server && npm run build` 均通过。生产预览 `http://127.0.0.1:4173/settings` 使用 Puppeteer 手机视口模拟已授权 Notification/PushManager 和后端 API，验证“发送测试通知”入口不存在，点击“通知提示”后 `PushManager.subscribe()` 被调用、开关变为开启并提交订阅。成功截图：`codex-pwa-settings-after-fix.png`、`codex-pwa-push-toggle-success.png`。
- 注意：本地自动化无法截取 Android 系统通知栏/锁屏的真实系统弹窗；上线后仍需用真机 Chrome PWA 登录、允许通知、退后台/锁屏验证服务端 Web Push 到达。
- 上线：2026-05-27 已执行 `npm run build` 并通过 FTP 上传前端 `dist` 到 `47.122.112.242`；线上首页已引用 `assets/index-BbjVz6MB.js` / `assets/index-CKjiVzIo.css`，`/sw.js` 包含 `xiaoxiang-pwa-v4`，`/api/health` 返回正常。

## PWA 后台/锁屏 Web Push

- 来源：用户确认“点开网页后可以正常推送通知”，但锁屏或不在小象日志网站中无法通知，说明前台 fallback 生效，仍需要确保服务端常驻 Web Push 生效。
- 状态：已补强后端。`server/src/lib/push.ts` 对 Web Push 发送增加 `TTL: 86400` 和 `urgency: high`；`server/src/routes/notifications.ts` 新增登录态排障接口 `GET /notifications/push/status` 和 `POST /notifications/push/self-test`，用于确认服务器 VAPID 配置、当前账号订阅数量、偏好和服务端主动推送是否可达。重新开启每日提醒或修改提醒时间时会清空 `lastDailyReminderDate`，方便同一天反复测试到点推送。
- 验证：`npm run lint`、`cd server && npm run build` 通过；已通过 FTP 上传 `server/dist`、`server/src`、`server/prisma/schema.prisma`、`server/package.json`、`server/package-lock.json`、`server/tsconfig.json` 到 `47.122.112.242`，未覆盖线上 `.env`；随后补传 `dist/routes/notifications.js` / `src/routes/notifications.ts` 的提醒时间重置修复。当前线上 `/api/health` pid 为 `7332`，上传后仍需在宝塔重启后端让新 dist 生效。

## PWA 每日提醒二次触发修复

- 来源：用户确认目标端为 Android Chrome/PWA，并说明“网页没打开/锁屏也要通知”；同时反馈每日写日记提醒第一次能触发，之后改时间不再触发。
- 状态：已补齐实现。前端本地兜底提醒去重从 `last_remind_date` 改为 `xiang_last_remind_<date>_<time>`，通知 tag 也包含日期和时间；设置页在开启提醒或修改提醒时间时清理当天本地提醒键，并同步服务端偏好。服务端 `PUT /notifications/preferences` 只在“提醒从关闭变开启”或“提醒时间真的变化”时清空 `lastDailyReminderDate`，避免 App 启动时同步 `dailyReminderEnabled: true` 导致当天重复推送。服务端 scheduler 的每日提醒 tag 同样加入提醒时间。
- 验证：`npm run lint -- --pretty false`、`npx tsc --noEmit -p server/tsconfig.json --pretty false`、根目录 `npm run build`、`server/` 下 `npm run build` 均通过。`git diff --check` 仍会报告 `src/App.tsx` 欢迎日记 Markdown 硬换行的尾随空格，这是既有欢迎文案排版差异，不属于本次通知逻辑。
- 注意：本地无法验证真实 Android 锁屏通知；上线后仍需真机登录 PWA，允许通知，调用 `/api/notifications/push/status` 和 `/api/notifications/push/self-test`，再测试好友申请/点赞/评论和改时间后的第二次每日提醒。

## 三项改动上传线上

- 来源：用户要求把截图中的三项改动上传到云端服务器：新增日志编辑纯色主题背景、修复云盘点击提示、增强互动与定时通知推送。
- 状态：已上传。重新执行根目录 `npm run build` 和 `server/` 下 `npm run build` 后，前端 `dist` 已通过 FTP 上传到 `47.122.112.242:/dist`，线上入口已引用 `assets/index-DBUmvdAy.js`；`sw.js` 已包含 `CACHE_VERSION = 'xiaoxiang-pwa-v4'`、`push` 和 `notificationclick`。后端已上传 `server/dist`、`server/src`、`server/prisma/schema.prisma`、`server/package.json`、`server/package-lock.json`、`server/tsconfig.json` 和 `server/scripts/doctor-cpamc.mjs` 到 `/xiaoxiang-server`，明确未上传/覆盖线上 `.env`。
- 验证：`curl -k -L http://47.122.112.242/` 可看到 `index-DBUmvdAy.js`；`curl -k -L http://47.122.112.242/sw.js` 可看到 PWA push/click 逻辑；`curl -k -L http://47.122.112.242/api/health` 返回 build `cpamc-only-20260520`、pid `7332`。
- 注意：后端文件已到服务器，但 `/api/health` pid 仍是上传前的 `7332`，说明 Node 进程尚未重启。互动与定时通知推送的后端新逻辑需要宝塔重启 `C:\wwwroot\xiaoxiang-server` Node 项目后才会生效。

## 云盘管理开发中提示

- 来源：用户说明云盘管理仍在开发中，点击侧边栏“云盘管理”时应提示功能正在开发中。
- 状态：已在 `src/components/Layout.tsx` 接入现有 `AppToast`，云盘管理按钮点击后显示“云盘管理功能正在开发中，敬请期待～”，不跳转到未完成页面。
- 验证：`npm run lint`、`npm run build` 通过；Puppeteer 手机视口打开首页侧边栏并点击云盘管理，toast 文案出现，截图为 `codex-cloud-wip-toast-success.png`。

## PWA 通知本地实测补修

- 来源：用户反馈锁屏/退出应用后无法触发通知，好友申请等互动通知在应用内也收不到；随后明确先不测 Android 安装包，先测试 PWA。
- 状态：已补修。`src/App.tsx` 的前台兜底轮询从仅好友申请扩展为好友申请、点赞、评论，并按本地开关和通知 ID 去重；排行榜点赞创建通知后也调用 `sendNotificationPush()`。本地 PWA 自动化测试发现 App 启动时会用本地默认 `21:00` 覆盖服务器已设置的提醒时间，已改为启动时只确保订阅，并且只在服务器尚未开启每日提醒而本地已开启时做一次迁移，不再覆盖服务器已有提醒时间。
- PWA 验证：本地后端用临时 VAPID 环境变量启动，Puppeteer 授权 `http://127.0.0.1:3000` 通知权限后注册 `/sw.js`、创建 `PushSubscription` 并提交 `/api/notifications/push/subscribe`；`/api/notifications/push/status` 返回 `configured: true` 且有订阅；`/api/notifications/push/self-test` 返回 `sentCount > 0`。将提醒时间改为当前分钟后，服务器偏好保持新时间，服务端 scheduler 随后把 `lastDailyReminderDate` 写为 `2026-05-27`。
- 互动验证：用本地测试账号调用 `POST /api/friends/request`，目标账号生成未读 `friend_request` 通知，后端日志无推送异常。
- 验证命令：`npm run lint -- --pretty false`、`npx tsc --noEmit -p server/tsconfig.json --pretty false`、根目录 `npm run build`、`server/` 下 `npm run build` 均通过。前端构建产物更新为 `assets/index-BRuGXoKy.js`。
- 上传：已将前端 `dist` 上传到 `47.122.112.242:/dist`，线上首页引用 `assets/index-BRuGXoKy.js`，`/sw.js` 包含 `xiaoxiang-pwa-v4`、`push` 和 `notificationclick`。已将后端 `server/dist`、`server/src`、`server/prisma/schema.prisma`、`server/package.json`、`server/package-lock.json`、`server/tsconfig.json`、`server/scripts/doctor-cpamc.mjs` 上传到 `/xiaoxiang-server`，明确未覆盖线上 `.env`。FTP 拉回 `/xiaoxiang-server/dist/routes/notifications.js` 确认包含 `/push/status`、`/push/self-test`、`/push/subscribe` 和 `lastDailyReminderDate` 逻辑。
- 注意：本次只做本地 PWA 链路验证，未继续 Android 安装包测试；线上 `/api/health` 仍返回 pid `7332`，说明后端文件已上传但 Node 进程尚未重启。仍需宝塔重启 Node，并确认 `.env` 已配置 `VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT` 后，用真实 Android Chrome/PWA 跑 `/api/notifications/push/status` 和 `/api/notifications/push/self-test`。
