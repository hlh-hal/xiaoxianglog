# 2026-07-17

## 月度回声双层下沉箭头

- 修改 `src/components/monthly-echo/MonthlyEchoExactPages.tsx` 与 `tests/monthly-echo.test.ts`：前 6 页共用双层 SVG 下沉箭头，保留原有 70px 底部点击热区、翻页和滑动逻辑。
- 主箭头使用 `translateY(0 -> 4px -> 0)`、透明度 `0.55 -> 0.85 -> 0.55`；残影使用 `translateY(-6px -> 2px -> -6px)`、透明度 `0 -> 0.22 -> 0`；两层均为 `2.2s ease-in-out infinite`。
- `prefers-reduced-motion: reduce` 下主箭头静止显示、残影隐藏；第 7 页不渲染按钮或 SVG。
- 后续真机截图发现原遮盖层与动态箭头共用位置：首页遮到愿望文案且没有盖住更下方的静态箭头，其他页面出现纯色遮罩色差。现已拆成独立图层：动态箭头保持原位，底图静态箭头用同一张底图左侧纯纸纹局部修补；第 7 页使用精确纸色补片并扩大到完整覆盖箭头。
- 浏览器 390x844 已检查首页文字完整、前 6 页只保留动态箭头、无明显修补色块，第 7 页完全无箭头。截图位于 `tmp/monthly-echo-emulator/monthly-arrow-clean-sample-page-1.png` 和 `monthly-arrow-page-7-clean-final.png`。
- Android 已执行 `npx cap sync android`、`gradlew assembleDebug`，覆盖安装到 `Pixel_8` 模拟器并通过打包 WebView 检查首页、第 4 页、第 6 页和第 7 页。最终首页截图为 `tmp/monthly-echo-emulator/monthly-arrow-home-pixel8-final.png`。
- 验证通过：`npm run lint`、`npm run test:monthly-echo`（24 项）、`npm run build`、Android debug 构建和安装。

## 月度回声箭头统一页面坐标

- 将动态箭头按钮从各自尺寸不同的 `.exact-artboard` 移到 `.exact-echo-page` 层；静态箭头修补层继续留在画板内，因此视觉箭头统一按整页底部定位，底图清理仍按各页素材坐标执行。
- 浏览器 390x844 实测第 1—6 页均为 `left=175px`、`top=798px`、距页面底部 `12px`；第 7 页无按钮、无修补层、无箭头。
- Pixel_8 打包 WebView 实测第 1—6 页相对坐标完全一致（缩放后 `left=124.41`、`top=567.3`、底部间距 `8.53`），并逐页确认没有原静态箭头。截图：`tmp/monthly-echo-emulator/monthly-arrow-fixed-position-pixel8.png`。
- 再次通过 `npm run lint`、`npm run test:monthly-echo`（24 项）、`npm run build`、Capacitor 同步、Android debug 构建与覆盖安装。

## 月度回声地图页细节修复

- 按底图原始像素重新测量 `.map-line-repair` 两端接点和切线，使用 `M 173.7 275.2 C 190 279, 217 298, 224.6 311.1` 连续补齐第一个节点到第二个节点之间的原生断口；颜色校准为 `#67713f`、线宽 `1px`，避免旧补线的错位、分叉和色差。
- 第一个节点文案从 `top:31%` 下移到 `top:33.5%`，保留 `left:12%`，避免标题、日期与节点路线重合。
- 静态箭头修补层保持 `z-index:29`，并从 `44×30px` 扩大到 `48×46px`，完整覆盖底图中距底部约 35px 开始的原静态箭头像素；纵向取样上移 24px，避开底图圆角外的白边，并使用椭圆径向遮罩羽化四周，消除修补层白色方块和矩形边缘。地图页底部只剩两层绿色动态箭头。
- 第 6 页 `.exact-recurring` 不再使用底图取样，修补层单独改用该页上下文纸色 `#f5eadd`，解决 Android Chrome 中仍可见的白色方块；第 1—5 页与第 7 页保持原逻辑。
- 第 4 页“三个关键时刻”的底图仍自带一个旧箭头；对 `.exact-moments .exact-artboard>img` 裁掉底部 4% 的纯纸张区域，彻底移除内部旧箭头，只保留画板外页面底部统一的双层动态箭头。
- 浏览器 390x844、DPR 3 与 Pixel_8 打包 WebView 均已检查。复修后的最终模拟器截图：`tmp/monthly-echo-emulator/map-fix-v2/map-feathered.png`。
- 验证通过：`npm run lint`、`npm run test:monthly-echo`（24 项）、`npm run build`、Capacitor 同步、Android debug 构建和覆盖安装。

## Android 微信登录与统一账号

- 新增微信外部身份体系：`User` 继续作为唯一主账号，`ExternalIdentity` 只把微信 OpenID/UnionID 映射到原 `userId`，不复制或合并日记；`ExternalAuthGrant` 以 token 哈希保存 10 分钟一次性新用户注册授权。
- 新用户必须微信授权后验证一个未注册邮箱、设置昵称与密码；已有邮箱在微信注册路径会被拒绝，并引导先邮箱登录后到“个人信息”绑定。绑定和解绑均需验证当前账号邮箱。
- 服务端新增 `/api/auth/wechat/config|login|register|binding|email-code|link|unlink`，微信 access token 只在服务端内存中即时使用；`WECHAT_LOGIN_ENABLED=false` 时 Android 登录页不会显示入口。
- Android 使用官方 OpenSDK `6.8.40`，新增 `XiangWechatPlugin` 与 `wxapi/WXEntryActivity`；覆盖未安装、取消、拒绝、超时、state 校验和进程重建后的短时结果恢复。普通 Web/PWA 不展示微信登录。
- 本地 SQLite 已先备份到仓库外 `C:\Users\ASUS\Documents\xiaoxiang-log-backups\wechat-login\xiaoxiang-before-wechat-20260717-152242.db`（SHA256 `4BF2B7D1082BFADF765F762DD6DF8665DECB41CB6B9E5A4A6AD3FE8A68390053`），再完成 `npm run db:push`；备份不留在仓库内，避免私密日记被误提交。
- 验证通过：`npm test`、`npm run test:wechat-auth`、`npm run lint`、前后端 build、`npm run android:sync`、Gradle Java 编译与 `assembleDebug`；390×844 浏览器验证微信注册页无横向溢出，Web 登录页无微信按钮。
- 尚未上线：需要先完成微信开放平台移动应用审核，登记包名 `com.xiaoxiang.diary` 与正式发布签名，再在服务器配置 AppID/AppSecret 并开启功能开关；真实授权必须用已安装微信的 Android 真机验收。

## 微信开放平台审核材料

- 在 `docs/wechat-open-platform-application-2026-07-17/` 准备了可直接提交的移动应用材料：28×28 与 108×108 PNG 图标、应用主要流程、微信登录/新用户注册流程、老用户绑定流程，以及 200 字以内的申请说明。
- 图标从当前 Android 正式启动图标确定性缩放，避免与安装后的应用图标不一致；三张流程图为 1600×1000 PNG，均小于 5MB。
- 从 2026-07-17 线上正式 APK 提取正式证书摘要：MD5 `9a0e0281cd8b3070c425c22290fd3eb4`、SHA-1 `2ef1799fa3d3ffe79be8d785427875ef30b00bb9`、SHA-256 `26dd01760803ea1672b0a0f19f8897067403b613530d82ab70d4c0ec7d0ae3cf`。微信登记与后续正式 APK 必须继续使用该证书，不能登记 debug 签名。
- 现有发布页记录 App 备案号为 `皖ICP备2026014410号-2A`，提交前仍需用户与工信部备案通知/后台逐字核对。官网字段建议用包含应用介绍和下载入口的 `https://hlh-hal.github.io/xiaoxianglog/`；自有域名 APK 链接只放审核说明。

## 月度回声首页底部重叠修复

- 首页原底图 `monthly-echo-cover-reference.png` 内烘焙了“愿你每个月都可以 / 收到自我回响”、装饰框、星点和静态下箭头，与页面层的双层动态箭头重叠。
- 新增 `public/monthly-echo/monthly-echo-cover-clean-v2.png`：只清除上述底部内容并用原有暖白纸张纹理补齐，原始参考图保留用于回退；`MonthlyEcho.tsx` 与 `MonthlyEchoExactPages.tsx` 已统一改用新版资源并增加缓存版本号。
- Android Pixel 8 模拟器通过本地设计页验证：首页底部祝福文案、装饰框、星点、静态箭头均消失，最底部动态绿色箭头保留。验证截图：`tmp/monthly-echo-emulator/monthly-echo-cover-cdp-verify-final.png`。
- 后续复查发现窄高屏按高度缩放时会在封面左右露出父容器底色；现已按封面 PNG 左右边缘分别采样纵向纸色渐变，只覆盖首页槽位背景，消除两侧色差且不影响其他页面。最终截图：`tmp/monthly-echo-emulator/monthly-echo-side-color-fixed-final.png`。
- 再次按 Pixel 8 截图校准顶部与右侧：顶部增加横向受光渐变、细纸纹理和 12px 羽化，右侧补充两段实测明度色阶与纸纹理；未复制封面图形，接缝上下取样已一致。最终截图：`tmp/monthly-echo-emulator/monthly-echo-top-right-final.png`。
- 验证通过：`npm run build`。仅有项目既存的大 chunk 与 `diaryService` 动静态混用警告。
