# 2026-07-21

## 心情趋势节点与详情交互微调

- 心情趋势详情卡在最后一次点击、键盘或拖动选择后保留 8 秒，再用 450ms 淡出；选中节点和垂直辅助线继续保留，后续再次选择会重新计时。
- 所有具有有效情绪分数的日期统一显示 `8px` 深绿色圆点、白色 `2px` 外圈，不再只突出最高点和最低点；无日志或无可识别情绪的数据日仍不显示节点并让曲线断开。
- 专项测试增加详情自动隐藏、有效日期节点完整性以及普通节点与极值节点样式一致性断言。验证通过：`npm test`、`npx tsx tests/profile-mood-trend.test.tsx`、`npm run lint`、`npm run build`、`git diff --check`。
- Android `Pixel_8_Codex` 已覆盖安装调试包并用连续七天长篇临时日志验收；周二至周一节点大小一致，点击详情显示后约 9 秒截图确认已消失。验证截图：`tmp/codex-mood-nodes-uniform.png`、`tmp/codex-mood-detail-visible.png`、`tmp/codex-mood-detail-hidden.png`。

## 心情详情卡改为“当天回顾”

- 详情卡不再展示情绪指数和分析日志数，改为“星期 · 情绪等级”、最多两行的当天回顾、最多三个主题标签和“查看当天日志 >”。摘要由前端纯函数从真实标题、正文和分块内容中提取，先清理 HTML、Markdown 与模板标题；证据不足时隐藏，具备多个可靠关键词时才使用中性兜底，不推断因果。
- `MoodTrendDay` 新增 `summary` 与 `entryIds`。单篇日志直接打开 `/editor?id=...`；同日多篇复用首页既有日期定位，通过 `/?date=YYYY-MM-DD` 展示对应日期，不新增页面、接口或数据库字段。
- Android WebView 实测：单篇周五正确打开 `codex-mood-preview-3`；多篇周一进入 `/?date=2026-07-20` 且能找到当天列表。320px 仿真下卡片左右均在视口内、页面横向溢出为 0、入口高度 36px；深色主题变量正确切换为深色卡片与浅色标题。
- 验证通过：`npm test`、`npm run lint`、`npm run build`。构建仍只有原有的动态导入和大包体积提醒。

## 从心情详情返回个人页的位置恢复

- 从“查看当天日志”进入单篇编辑器或多篇当天列表前，记录个人页自身滚动容器的位置；返回 `/profile` 后在路由动画稳定时恢复并立即清理临时值，避免回到个人页顶部或影响以后正常进入个人页。
- Android WebView 实测从个人页 `scrollTop=900.19` 打开周五单篇日志 `codex-mood-preview-3`，返回后仍为 `900.19`，且一次性恢复值已清理。
- 同时保存进入日志前选中的趋势日期；返回后不再回到默认最近一天。Android WebView 实测打开“周六 · 低落”后返回，卡片仍为“周六 · 低落”、选中日期仍为 `2026-07-18`，滚动位置仍为 `900.19`，两个一次性恢复值均已清理。

## 导出日记支持日期范围

- 设置页“导出日记”不再立即生成文件，改为先打开现有风格的底部弹窗；默认“全部时间”，也可选择自定义开始/结束日期，取消不触发导出。
- 自定义范围按日记领域日期 `diaryDate` 的本地自然日过滤，开始日使用 `00:00:00.000`，结束日使用 `23:59:59.999`，两端均包含；非法范围和未来结束日期会禁用导出并显示原因，空结果不生成文件。
- 原 Markdown 排序、内容格式、图片处理、文件名以及本地 Vault/浏览器保存链路未改；导入备份和本地日志同步未改。
- 验证通过：`npx tsx tests/export-date-range.test.ts`、`npm run lint`、`npm run build`、`git diff --check`。Puppeteer 在 390×844 视口验证默认选项、自定义日期输入、非法范围提示/禁用态和底部按钮完整可见；截图为 `tmp/export-range-all.png`、`tmp/export-range-custom.png`。

## 日志圈卡片正文扩展到 7 行

- 日志圈推荐/好友列表共用的帖子卡片正文截断上限由 5 行调整为 7 行，其他日记列表的展示行数不变。
- 验证通过：`npm run lint`、`npm run build`；构建产物已生成 `line-clamp-7` 样式。

## Android v1.0.27 用户端发布

- 已发布 `1.0.27 / versionCode 29`。公告涵盖：每日回声仅展示服务端验收合格稿、个人页心情趋势与当天回顾、日期范围导出；修复月度回声长行动标题、历史情绪/长文显示，以及当天回顾返回位置恢复。
- 验证通过：`npm run lint`、完整 `npm test`、前端/后端 build、`android:sync`、release 构建、v2/v3 签名、备案证书 MD5、`aapt` 包信息。公网 APK SHA256 为 `5175B51C5F45DF5AAE28E98E47641FAF3E07BCFA1AE44ACF170D33693DDF7AB4`，与本地一致；线上 manifest 为 `1.0.27 / 29`。
- FTP 极不稳定：全量前端和后端上传均超过 5 分钟；前端入口、主 JS/CSS 和本次月度情绪素材已分批上传。APK 已上传至主/备用下载路径并验证成功。
- 后端 `dist/index.js`、`dailyEchoCore.js`、`dailyEchoService.js`、`monthlyEchoService.js`、`monthlyEchoUtils.js` 已上传；`monthlyEchoV2.js` 多次 FTP 失败，**当前不要重启线上 Node**。待该文件补传成功后再重启 `C:\wwwroot\xiaoxiang-server`，否则新的月度回声 V2.10 不能完整生效。
