# 2026-06-24

## Android 编辑器顶部遮挡与白色光标块追补

- 来源：用户将 6 月 23 日改动推送到用户端后反馈：编辑页顶部出现正文与日期/按钮重叠；Android App 内选中文字仍有白色光标/方块，拖选卡顿；同一页面在手机浏览器本地测试正常。
- 判断：6 月 23 日新增的 Android-only `diary-editor-scrollport` 选区样式（`contain: paint` / `isolation` / 选区态背景）没有解决原生 WebView 选区层，反而增加了 contenteditable 合成复杂度；顶部重叠来自编辑态/选区态关闭 mask 后，正文可直接滚到透明导航区域后方。
- 改动：回退 `src/index.css` 里 6 月 23 日新增的 Android 选区 CSS，保留原有 ProseMirror selection 与白色 inline background 透明化规则。
- 改动：`src/pages/Editor.tsx` 新增固定顶部遮挡层，位于正文滚动层之上、顶部导航之下，复用当前主题背景图/背景色和 overlay，使正文滚到日期栏区域时按用户视频效果自然消失，不再压住日期和按钮。
- 改动：精简文字选区滚动 guard，移除拖选/滚动期间的多帧 `requestAnimationFrame` 修正；选区滚动时只延迟释放，松手后做一次底部空白兜底裁剪，减少 Android 选区手柄拖动时的 JS 拉扯。
- 改动：`android/app/src/main/java/com/xiaoxiang/diary/MainActivity.java` 对 WebView 关闭 force dark / algorithmic darkening，降低 Android WebView 在深色主题、透明层或原生选区层下把底色合成为白色的概率。
- 验证：`npm run lint` 通过；`npm run build` 通过，仅保留既有 dynamic import 与 chunk size 提醒；`npx cap sync android` 完成；临时设置 `JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot` 后 `android/.gradlew.bat :app:assembleDebug` 通过，仅提示 `setForceDark` 相关 API deprecated。
- 风险/下一步：仍需 Android 真机确认选区手柄白块是否完全消失；如果特定 WebView/输入法仍复现，应记录机型、Android WebView 版本和输入法，转向原生 WebView/系统选区层兼容处理，不再继续堆叠 contenteditable CSS。

## Android 编辑器光标/选区手柄白底原生资源修复

- 来源：用户按 `docs/android-editor-cursor-handle-white-block-plan.md` 要求继续修复 Android 编辑器光标下坠/选区手柄周围白色矩形块；用户接受绿色下坠手柄本体，但不接受白色区域覆盖任意日记背景。
- 判断：截图中的白块更像 Android WebView/系统文本选择手柄资源自带的不透明画布，而不是普通 DOM 背景；继续堆叠 ProseMirror CSS 风险较高。
- 改动：新增 `android/app/src/main/res/drawable/text_select_handle.xml`、`text_select_handle_left.xml`、`text_select_handle_right.xml`，使用透明 vector 画布，只绘制小象主色 `#446733` 的插入光标手柄和左右选区手柄。
- 改动：`android/app/src/main/res/values/styles.xml` 在 `AppTheme.NoActionBar` 绑定 `android:textSelectHandle*` 三个资源，保留系统复制/粘贴/全选菜单和原生拖动能力，不改 React 编辑器逻辑。
- 验证：临时设置 `JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot` 后，`android/.gradlew.bat :app:assembleDebug` 通过；`npm run lint` 通过。
- 风险/下一步：仍需安装到 Android 真机验证单光标下坠、长按左右手柄、中文输入法组合输入、复制/粘贴/全选菜单，以及深色/图片主题背景是否不再被白块破坏。
## Android 发布 skill 流程优化

- 来源：用户要求按 `ANDROID_RELEASE_SKILL_OPTIMIZATION_PLAN.md` 迭代 `xiaoxiang-android-release` skill，解决上次发布耗时 28 分钟暴露出的流程问题。
- 改动：更新 `C:\Users\ASUS\.codex\skills\xiaoxiang-android-release\SKILL.md` 和 `references\release-workflow.md`，把项目根目录 guard 固化为 `D:\小象日志`，明确发布默认只走自有服务器，GitHub 仅在用户显式要求时处理。
- 改动：把理想化的 5-8 分钟目标调整为正常 8-12 分钟；写入实际时间预算：lint 30-45s、android sync 15-30s、assembleRelease 50-90s、前端 FTP 90-130s、APK 上传 60-240s。
- 改动：固定版本同步组为 `android/app/build.gradle`、`src/config/appRelease.ts`、`public/app-update.json`、`docs/app-update.json`、本地 `docs/index.html`；要求版本只决策一次，避免重复 bump。
- 改动：明确 PowerShell 禁用 bash heredoc；APK 上传必须设置 300 秒外层超时，不再先用 120 秒；`verify_public_release.ps1` 成功后不再重复做大量线上探测。
- 验证：`PYTHONUTF8=1 python C:\Users\ASUS\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\ASUS\.codex\skills\xiaoxiang-android-release` 通过；只读 grep 确认 8-12、300 seconds、Project Root Guard、GitHub skip、heredoc ban 等关键规则已写入。
