# 2026-07-16

## 月度回声第七页真机布局修复

- 修改 `src/components/monthly-echo/MonthlyEchoExactPages.tsx`。
- 删除独立绿色结论字块，把 `finalInsight` 去重后追加为正文最后一段。
- 合并原先上下两个绝对定位正文区，改为一个自适应高度、按文字总量切换密度的连续正文容器。
- 使用右侧多段 `shape-outside` 轮廓避让照片、花束和花茎；正文最大高度在署名安全区上方结束。
- 回声信画板右移 10px，增加左侧安全边距并露出左上胶带；落款文字和小象图标向左移动 8%，小象完整显示。
- 使用 `mix-blend-mode: darken` 消除移动后的落款图片纸色矩形边界。

## 验证

- `npm run lint`：通过。
- `npm run test:monthly-echo`：23 项通过。
- `npm run build` / `npm run android:sync`：通过，只有既有的大 chunk 警告。
- `android\\gradlew.bat assembleDebug --no-daemon --console=plain`：通过。
- Pixel_8 模拟器覆盖安装 `1.0.22` 成功；448 字长文测得正文 `clientHeight=scrollHeight=576`，末段完整且与署名保留约 64px 安全距离。
- 最终截图：`tmp/monthly-echo-emulator/monthly-letter-signature-darken.png`。

## 环境提示

- 当前全局 `JAVA_HOME` 指向不存在的 `D:\\Program Files\\Java\\jdk-23`；本次 Android 构建临时使用 `C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.11.10-hotspot`，未修改全局环境变量。
- Pixel_8 AVD 一度仅剩 184MB，安装 APK 报 `Requested internal only, but not enough space`；清理模拟器内 Chrome、Photos、Play Store、Google Play Services 等测试数据后恢复安装。此操作只影响模拟器。

## 月度回声行动轨迹空白修复

- 修改 `server/src/lib/monthlyEchoV2.ts`、`server/src/lib/monthlyEchoService.ts`、`server/src/lib/monthlyEchoUtils.ts`、`src/components/monthly-echo/MonthlyEchoExactPages.tsx` 和 `tests/monthly-echo.test.ts`。
- 扩充可观察行动识别，并加入基于 DailyTraceNode 有效证据的确定性回填；按证据 ID 去重，避免同一日志原句被 AI 和回填逻辑重复展示。
- 真实 7 月报告运行态验证：状态 `ready`，行动 6 条，包含表达交付时间、求助、清理待办、拒绝临时会议、短时运动和提前规划；第五页移动端截图中展示前 5 条，布局无溢出。
- 验证通过：`npm run test:monthly-echo`（24 项）、`npm run lint`、前端 `npm run build`、后端 `npm run build`、`git diff --check`。截图：`tmp/monthly-echo-runtime/actions-ready-390x844.png`。

## 月度回声第七页右侧装饰微调

- 仅修改 `src/components/monthly-echo/MonthlyEchoExactPages.tsx` 的回声信装饰图层，正文、左侧画布位置、字号和流式布局保持不变。
- 照片纸条与花枝纸条改用局部原图补偿层向左收拢，避免移动整张画布导致左侧再次被裁切。
- 署名和小象拆分为同源参考图切片，并对底部切片轻微横向收缩；小象完整落入安全区，署名区域不再使用纯色小象遮罩。
- 删除局部补偿层带回的浅绿块，并用同一纸张素材的空白纹理加羽化遮罩覆盖旧结论底图，降低纸张色差和矩形边界。
- 最终视觉截图：`tmp/monthly-echo-emulator/monthly-letter-right-fix-browser-final4.png`（411×891、2x DPR）；右侧两处纸条、花朵、小象均完整。
- 验证通过：`git diff --check`、`npm run lint`、`npm run test:monthly-echo`（23 项）、`npm run build`、`npx cap sync android`、`android\\gradlew.bat assembleDebug`；最终 debug APK 已覆盖安装到 `emulator-5554`。
