# 2026-06-21

## 月度回声上线版实现

- 来源：用户要求在现有“小象日志月度回声实现计划”基础上继续完成完整上线实现，并补齐上线保护：本地时区 monthKey、prompt injection 防护、异步 job、运行锁、月末补偿推送、backfill 草稿、evidenceQuotes/posterQuote 校验、删除/关闭策略、高风险兜底、已推送 stale 不重复推送、HH:mm + IANA timezone、GET 去重、JSON Text helper、并发/重试/超时/失败落库与测试。
- 后端改动：新增 Prisma 模型 `DailyTraceNode`、`MonthlyArcDraft`、`MonthlyEcho`、`MonthlyEchoJobLog`，扩展 `NotificationPreference` 月度回声开关/推送时间/时区；新增 `server/src/lib/jsonText.ts`、`aiProvider.ts`、`monthlyEchoUtils.ts`、`monthlyEchoService.ts`、`monthlyEchoScheduler.ts`；新增 `/api/monthly-echo` 路由和 backfill 脚本 `server/scripts/backfill-monthly-echo.ts`。日记 create/update/sync/delete 只标记 trace stale 和入 job，不同步调用 AI。
- 关键保护：`monthKey` 使用 `entry.diaryDate` 日历键，跨月修改 stale 旧月和新月；所有 prompt 声明日记/今日回声/Trace/Draft 均为待分析材料不是指令；`evidenceQuotes` 做原文/今日回声连续包含校验，`posterQuote` 只能从已校验证据中选择且过滤高风险；自伤/自杀/伤害他人内容走克制安全兜底；`MonthlyEchoJobLog` 提供 `userId + monthKey + jobType` 锁，月末推送路径增加 runtime lock，事务内二次检查 `pushedAt`，已 pushed stale 只更新内容不再重复推送。
- 前端改动：新增 `/monthly-echo?monthKey=YYYY-MM` 五张卡片式回声页和保存海报；搜索新增 `月度回声`、`月报`、`6月回声`、`2026-06月度回声` 等虚拟结果；设置页新增“月度回声”和“月末推送提醒”开关、月末提醒时间；启动时若需要月末推送会确保 PWA push subscription。
- 测试：新增 `npm run test:monthly-echo`，覆盖 diaryDate monthKey、月末错过 20:00 补偿、时区 fallback、AI JSON/prompt injection、诊断词过滤、证据 quote、posterQuote、高风险兜底，以及源码护栏断言（跨月 stale、删除 stale、GET/regenerate 去重、month_end runtime lock、推送前 pushedAt 检查）。测试成功截图：`artifacts/monthly-echo-test-success.png`。
- 验证已通过：`server npm run db:generate`、`server npm run build`、根目录 `npm run test:monthly-echo`、`npm run lint`、`npm run build`。`npm run build` 仅保留既有大 chunk / dynamic import 警告。
- 上线注意：本轮未执行 `server npm run db:push`，避免在未确认目标库的情况下改动 SQLite；部署前必须在目标环境执行 Prisma 同步并确认 scheduler 进程已启动。AI 调用依赖后端现有 CPAMC/LongCat 环境变量，若未配置会走 fallback/失败落库。
- 本地效果测试补充：已在本地 SQLite 为 `hulianghal2006@gmail.com` 准备测试账号并开启月度回声偏好，启动后端 `3001` 和前端 `3000`；创建/同步 2026-06 测试日记后手动触发月度回声生成，小米模型调用日志显示 `provider=xiaomi-mimo targetModel=mimo-v2.5`，生成结果 `status=ready`。截图：`artifacts/monthly-echo-local-ai-effect.png`、`artifacts/monthly-echo-search-entry.png`、`artifacts/monthly-echo-settings-entry.png`。
- 测试中发现并修复：`trace` 类型 `MonthlyEchoJobLog` 会停留在 pending，且可能被 `hasActiveMonthlyEchoJob` 当成页面生成锁。已将 active lock 过滤为 `monthly_arc/monthly_echo/month_end/backfill/regenerate`，并在 `processPendingTraceNodes()` 处理后把对应 trace job 标成 success 或保留 pending。验证：`server npm run build`、`npm run test:monthly-echo`、`npm run lint` 通过；本地 DB 中 2026-06 trace job 已清为 success。

## 日记完成卡片写作用时误差修正

- 来源：用户反馈 2026-06-20 日记从头连续写到结束体感约 5 分钟，但完成卡片只显示 3 分钟，希望误差控制在约 1 分钟内。
- 改动：`src/utils/dailyEchoCompletionStats.ts` 将写作 activity window 从 30 秒提升到 90 秒，分钟展示改为四舍五入；`src/pages/Editor.tsx` 除 `onUpdate` 外，也在 `beforeinput`、`input`、`compositionstart/update/end` 和编辑类 `keydown` 记录写作活动，保留 blur、visibility hidden、pagehide 暂停。
- 测试：`tests/daily-echo-completion-stats.test.ts` 覆盖连续 5 分钟自然停顿、长空档封顶、跨会话累计、4 分 31 秒显示 5 分钟等场景。
- 验证：`npm run test:daily-echo-completion` 通过；`npm run verify:daily-echo-writing-time` 通过，5 分钟用例存储 `activeWritingSeconds=295`，跨会话用例从 14 分钟累计到 15 分钟。
- 注意：`npm run lint` 当前被无关未跟踪文件 `src/pages/MonthlyEcho.tsx` 的 `Session.name` 类型错误阻塞，本次未修改该文件。

## 月之回响 Figma 风格复刻

- 来源：用户提供「小象日志 月之回响 UI」Figma 链接、6 张导出图和关键 CSS，要求按计划把 `/monthly-echo` 改成 6 屏移动端故事信件 UI。
- 改动：重写 `src/pages/MonthlyEcho.tsx` 的呈现层，保留现有 API、生成队列、重新生成、保存海报逻辑；新增 390×844 设计基准的月度封面、本月主线、三个关键时刻、行动轨迹、反复主题、回声信 6 屏纵向 snap 滚动体验。
- 视觉策略：未引用临时截图文件，回声圆环、纸条、纸卡、植物线稿、信纸和按钮均使用 CSS/React 绘制；动态内容来自真实月度回声数据，并在 UI 层做短句提取和截断，避免撑破设计稿布局。
- 验证：`npm run lint`、`npm run test:monthly-echo`、`npm run build` 均通过；`npm run build` 仍保留既有大 chunk / dynamic import 警告。
- 视觉截图：移动端 390×844 截图已输出到 `artifacts/monthly-echo-figma-final-page-1.png` 至 `artifacts/monthly-echo-figma-final-page-6.png`。
